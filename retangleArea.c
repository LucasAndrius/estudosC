#include <stdio.h>

int main()
{

  // varibles
  int length = 4;
  int width = 6;
  int area;

  // calculate th area of retangle
  area = length * width;

  // print the variables

  printf("Length is: %d\n", length);
  printf("Width is: %d\n", width);
  printf("Area of the retangle is: %d", area);
}
